<!--
Adding margins to App Widgets:
https://developer.android.com/guide/topics/appwidgets/#CreatingLayout
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/widget_margin">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/colorWidgetBackground"
        android:padding="8dp">

        <ImageView
            android:id="@+id/appwidget_logo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_alignParentLeft="true"
            android:layout_centerVertical="true"
            android:contentDescription="@string/appwidget_logo_content_description"
            android:scaleType="fitStart"
            android:src="@drawable/ic_widget_logo" />

        <ImageView
            android:id="@+id/appwidget_toggle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:layout_gravity="center"
            android:contentDescription="@string/appwidget_toggle_content_description"
            android:src="@drawable/ic_widget_toggle_off" />

    </RelativeLayout>
</FrameLayout>